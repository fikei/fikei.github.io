// ============================================
// Clustering — k-means++ on TF-IDF vectors
// Will be implemented by data-engine agent
// ============================================

import type { Pin, Cluster, GraphEdge } from './types';

export function buildClusters(_pins: Pin[]): Cluster[] {
  return []; // TODO: implement
}

export function buildEdges(_clusters: Cluster[]): GraphEdge[] {
  return []; // TODO: implement
}
